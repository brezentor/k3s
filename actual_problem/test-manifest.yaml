apiVersion : v1
kind : Pod
metadata :
  name : my-web-test
  labels:
    env : test
    owner : brut
spec :
  containers :
    - name : nginx-cont
      image: nginx:latest
      ports:
        - containerPort : 1234
